// 裁判年度總覽（共用模組）

function openJudgeDashboard(userId) {
  alert('準備開啟裁判年度總覽，裁判 ID = ' + userId);

  // ✅ 下一步才會做的事（現在先不用）
  // 1. 開啟年度總覽 modal
  // 2. fetch ?action=getJudgeAnnualDashboard&user_id=...
  // 3. render KPI / 月份 / 站位
}
